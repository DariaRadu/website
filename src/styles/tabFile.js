import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    tabContainer: {
        minHeight: '90vh',
    },

    tabTitle: {
        fontSize: '50px',
        color: '#5d576b',
    }

})

export default useStyles;
