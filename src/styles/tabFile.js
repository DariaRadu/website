import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    tabContainer: {
        minHeight: '55vh',
    },

    tabTitle: {
        fontSize: '30px',
    }

})

export default useStyles;
