import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    App: {
        tabMenu: {
            width: '100vw'
        },
        body: {
            color: '#5d576b'
        }
    }

})

export default useStyles;
