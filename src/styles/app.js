import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    App: {
        tabMenu: {
            width: '100vw'
        },
        body: {
            color: 'white'
        }
    }

})

export default useStyles;
